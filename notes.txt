`https://graphql.contentful.com/content/v1/spaces/:spaceid`,

0. Init
  a. Create contenful account
  b. Tour
  c. Add envs
  d. GraphQL
  e. create fetcher
1. Dynamic content
  z. data modeling
  a. Hero
  b. Sections
  c. Links
  d. Nav
  c. Layout order
2. Assets
  a. Logo wall
  b. Next.js image config
3. Dynamic paths
  a. Create Static paths
  b. Pre render routes
4. Preview
  a. Add preview to fetcher
  b. Setup preview contenful
  c. Setup draft mode next.js route handlers
  d. Preview Hero

#ASSET MEDIA CONTENTS - general
query AssetCollection {
   assetCollection {
     items {
       title
       url
       height
       width
     }
   }
 }

#ASSET MEDIA CONTENTS - only dynamic files by name
query AssetCollection($where: AssetFilter) {
  assetCollection(where: $where) {
    items {
      title
      url
      width
      height
    }
  }
}

{
  "where": {
    "title_contains": "client"
  }
}

#Navigation header links
query Query($where: NavigationFilter) {
  navigationCollection(where: $where) {
    items {
      name
      linksCollection {
        items {
          label
          link
        }
      }
    }
  }
}

{
  "where": {
    "name": "Header"
  }
}